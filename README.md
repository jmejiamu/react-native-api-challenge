## react-native-api-challenge

## Used

#### clone the repo and cd to the folder
#### npm install
#### Use PostgreSQL
#### Create a database called "menu"
#### Create a table called "userdata" with the columns id serial primary key, name TEXT, last_name TEXT, phone TEXT
